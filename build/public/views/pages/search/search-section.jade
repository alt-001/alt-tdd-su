#search(ng-controller='searchCtrl')
  .title
    | Please type here for a lovely thing...
  input(type='text' ng-model='query' autofocus)
  .results(ng-show='query.length >= 1')
    .section-results
      ul
        li.col-xs-2(search-results ng-repeat='product in products | filter: {$: query} | orderBy: section')